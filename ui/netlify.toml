[build]
  command = "pnpm build"
  publish = "dist"

[[edge_functions]]
  function = "hiro-proxy"
  path = "/hiro/*"

# SPA fallback for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
